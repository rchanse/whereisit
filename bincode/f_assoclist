#        Format of lines below
#  can use vim   :% su/fubar/fubar/g
#  in this file change fubar to  your exec-lib (fubar ??)
# 0. associate filename  up to 3 override parms, if none/null = editor
# 1. pound in col 1 is comment
# 2. to insure vim-edit: use  f assoc sudo vim
# 3. :sort  will 'sort' this file
#----
h   /etc/hosts
hedit  /etc/hosts sudo vim
heditn /etc/hosts sudo nano
hn  /etc/hostname
os  /etc/os-release
p   /etc/passwd less
sh  /etc/shadow
myp ~/.profile
T       ~/tempname a b c
a a  
a1 a p1  
a2 a p1 p2 
a3 a p1 p2 p3
ap       ~/whereisit/addpath
build    ~/whereisit
t       tempname
v     ~/.vimrc
vimrc ~/.vim/vimrc
